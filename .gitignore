# Local-only runtime state — never commit
state/
**/state/blueprint-secrets.txt
**/state/processes.json
**/state/agent-logs/
**/appsettings.Local.json
**/.env.local
/.env

# a365 CLI generated state — contains the blueprint client secret (DPAPI-protected).
# The CLI itself warns: "do not commit them to source control!"
**/a365.generated.config*.json
a365.generated.config*.json

# Local tenant registry with YOUR real IDs — keep private. Commit tenant-state.example.yaml instead.
/tenant-state.yaml
# a365 CLI bootstrap config (carries real tenant/app ids); regenerated by the CLI.
**/a365.config.json

# .NET
bin/
obj/

# Node
node_modules/
.next/

# IDE
.vs/
.vscode/

# Logs
*.log
.install-az.log
